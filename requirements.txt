# =====================================================
# NeuroDrums AI — Requirements
# =====================================================

# --- Core Scientific ---
numpy>=1.26
scipy>=1.12
scikit-learn>=1.5.0

# --- Audio I/O & Processing ---
librosa>=0.10.2
soundfile>=0.12.1
sounddevice>=0.5.1
ffmpeg-python>=0.2.0

# --- UI Framework ---
PySide6>=6.7.0

# --- AI / ML ---
# PyTorch (CPU or GPU) - install GPU version manually if needed:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
demucs>=4.0.1

# ONNX Runtime (GPU preferred on Windows, CPU fallback)
onnxruntime-gpu>=1.18; sys_platform=="win32"
onnxruntime>=1.18; sys_platform!="win32"

# --- Utilities ---
packaging>=24.0
requests>=2.31.0

# --- Legacy (kept for audio editor transforms) ---
# tkinterdnd2 is no longer required with PySide6
